# M3.16 — Visual reset

Rupture avec le dark+ambre : **outil clair graphite**, accent **bleu-acier** `#2F6FED`, vert uniquement pour Running.

## Livré

- Tokens light (`--bg` `#F4F5F7`, cards blanches, texte slate)
- Shell / dock / statusbar / boutons / selects adaptés
- Clicker : badges, status, preview, hotkey sous le nouveau thème
- Layout fluide M3.15 conservé

## Vérif

```bash
npx tsc --noEmit
npm run tauri dev
```
