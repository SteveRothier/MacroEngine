<Window x:Class="MacroEngine.UI.ZoneSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sélection de Zone"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        Cursor="Cross">
    <Canvas x:Name="SelectionCanvas" Background="#40000000">
        <Rectangle x:Name="SelectionRectangle"
                   Stroke="Red"
                   StrokeThickness="2"
                   StrokeDashArray="5,5"
                   Fill="#20FF0000"
                   Visibility="Collapsed"/>
        <TextBlock x:Name="CoordinatesTextBlock"
                   Foreground="White"
                   Background="#80000000"
                   Padding="8,4"
                   FontSize="12"
                   FontWeight="Bold"
                   Visibility="Collapsed"/>
    </Canvas>
</Window>
