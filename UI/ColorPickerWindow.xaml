<Window x:Class="MacroEngine.UI.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pipette Couleur"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="220" Height="160">
    <Border Background="#E0FFFFFF"
            BorderBrush="#FF0078D4"
            BorderThickness="2"
            CornerRadius="8"
            Padding="12">
        <StackPanel>
            <TextBlock Text="🎨 Pipette Couleur" 
                       FontWeight="Bold" 
                       FontSize="14"
                       Margin="0,0,0,8"/>
            
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border x:Name="ColorPreviewBorder"
                        Grid.Column="0"
                        Width="40" Height="40"
                        BorderBrush="#FF666666"
                        BorderThickness="1"
                        Margin="0,0,8,0"
                        Background="Black"/>
                
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="XTextBlock">
                        <Run Text="X: "/>
                        <Run x:Name="XValue" Text="0" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock x:Name="YTextBlock">
                        <Run Text="Y: "/>
                        <Run x:Name="YValue" Text="0" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock x:Name="ColorTextBlock">
                        <Run Text="Couleur: "/>
                        <Run x:Name="ColorValue" Text="#000000" FontWeight="Bold"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
            
            <TextBlock Text="Cliquez pour sélectionner • ESC pour annuler" 
                       FontSize="10"
                       FontStyle="Italic"
                       Foreground="#FF666666"/>
        </StackPanel>
    </Border>
</Window>
