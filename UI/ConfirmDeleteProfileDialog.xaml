<Window x:Class="MacroEngine.UI.ConfirmDeleteProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Confirmer la suppression"
        Width="420" Height="240"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundPrimaryBrush}">
    <Border x:Name="RootBorder"
            Background="{StaticResource BackgroundPrimaryBrush}"
            BorderBrush="{StaticResource BorderLightBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Barre de titre -->
            <Border x:Name="TitleBarBorder" Grid.Row="0"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Background="{StaticResource BackgroundSecondaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="16,0,0,0" Height="44"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="&#xE18E;" FontFamily="{StaticResource FontLucide}" FontSize="18"
                                   Foreground="{StaticResource ErrorBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Confirmer la suppression" Style="{StaticResource TextHeading}"
                                   Foreground="{StaticResource TextPrimaryBrush}" FontSize="15"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="CloseButton" Style="{StaticResource WindowCaptionCloseButtonStyle}"
                                ClickMode="Press" Focusable="False" Foreground="{StaticResource TextPrimaryBrush}"
                                FontFamily="{StaticResource FontLucide}" Content="&#xE1B2;"
                                ToolTip="Fermer" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Message + checkbox -->
            <Border Grid.Row="1" Margin="20,16,20,12"
                    Background="{StaticResource BackgroundSecondaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="MessageText" TextWrapping="Wrap"
                               Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" Margin="0,0,0,16"/>
                    <CheckBox x:Name="SkipConfirmationCheckBox"
                              Content="Ne plus demander pour cette session"
                              Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"
                              IsChecked="False"/>
                </StackPanel>
            </Border>

            <!-- Boutons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="20,0,20,20">
                <Button x:Name="DeleteButton" Style="{StaticResource ButtonPrimary}"
                        Content="Supprimer" Click="DeleteButton_Click"
                        Padding="14,8" Margin="0,0,8,0"
                        ToolTip="Supprimer le profil"/>
                <Button x:Name="CancelButton" Style="{StaticResource ButtonSecondary}"
                        Content="Annuler" Click="CancelButton_Click"
                        Padding="14,8"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
