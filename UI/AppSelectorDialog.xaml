<Window x:Class="MacroEngine.UI.AppSelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Applications cibles"
        Height="680" Width="640"
        MinHeight="560" MinWidth="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource BackgroundPrimaryBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <!-- Style pour la barre d'en-têtes des colonnes (Processus, Fenêtre, PID) -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource BackgroundSecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTip" Value="Cliquer pour trier"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- TextBox sans bordure (focus ne change pas la taille), pour barre de recherche intégrée -->
        <Style x:Key="TextBoxSearchInBar" TargetType="TextBox" BasedOn="{StaticResource TextBoxModern}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="0">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Bouton refresh dans la barre : hover discret + rotation au clic -->
        <Style x:Key="RefreshButtonInBarStyle" TargetType="Button">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter x:Name="iconPresenter"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RenderTransformOrigin="0.5,0.5"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}">
                                <ContentPresenter.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0CFFFFFF"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="iconPresenter"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         From="0" To="-360"
                                                         Duration="0:0:0.35"
                                                         FillBehavior="Stop"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Bouton supprimer des chips (sélection actuelle) : croix rouge pourpre au hover -->
        <Style x:Key="ChipRemoveButtonStyle" TargetType="Button">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="Transparent"
                                CornerRadius="3"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="iconTb"
                                       Text="&#xE1B2;"
                                       FontFamily="{StaticResource FontLucide}"
                                       FontSize="10"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="iconTb" Property="Foreground" Value="#C25B6B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border x:Name="RootBorder"
            Background="{StaticResource BackgroundPrimaryBrush}"
            BorderBrush="{StaticResource BorderLightBrush}"
            BorderThickness="1"
            Focusable="True"
            ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Barre de titre -->
            <Border x:Name="TitleBarBorder"
                    Grid.Row="0"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Background="{StaticResource BackgroundSecondaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="16,0,0,0"
                    Height="44"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="Applications cibles"
                                   Style="{StaticResource TextHeading}"
                                   FontSize="15"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="CloseButton"
                                Style="{StaticResource WindowCaptionCloseButtonStyle}"
                                Click="CloseButton_Click"
                                ClickMode="Press"
                                Focusable="False"
                                Foreground="{StaticResource TextPrimaryBrush}"
                                FontFamily="{StaticResource FontLucide}"
                                Content="&#xE1B2;"
                                ToolTip="Fermer"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Contenu : grille logique (liste = zone principale) -->
            <Grid Grid.Row="1" Margin="20,16,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Liste des processus (zone principale, scroll interne) -->
                <Border Grid.Row="0"
                        Background="{StaticResource BackgroundTertiaryBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource RadiusPanel}"
                        Padding="12"
                        Margin="0,0,0,12"
                        MinHeight="200"
                        ClipToBounds="False">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                   Text="Processus en cours d’exécution"
                                   Style="{StaticResource TextHeading}"
                                   FontSize="13"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                            <Border x:Name="SearchBarBorder" Grid.Column="1" Width="220" MinWidth="160" Margin="8,0,0,0"
                                    Background="{StaticResource BackgroundPrimaryBrush}"
                                    BorderBrush="{StaticResource BorderLightBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource RadiusTextInput}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0" Margin="4,0,0,0">
                                        <TextBlock x:Name="SearchPlaceholder"
                                                   Text="Rechercher..."
                                                   Style="{StaticResource TextMuted}"
                                                   FontSize="12"
                                                   IsHitTestVisible="False"
                                                   VerticalAlignment="Center"
                                                   Margin="2,0,0,0"/>
                                        <TextBox x:Name="SearchTextBox"
                                                 Style="{StaticResource TextBoxSearchInBar}"
                                                 Background="Transparent"
                                                 Foreground="{StaticResource TextPrimaryBrush}"
                                                 CaretBrush="{StaticResource TextPrimaryBrush}"
                                                 FontSize="12"
                                                 Padding="0,2,0,2"
                                                 VerticalAlignment="Center"
                                                 ToolTip="Rechercher par nom de processus ou titre de fenêtre"
                                                 GotFocus="SearchTextBox_GotFocus"
                                                 LostFocus="SearchTextBox_LostFocus"
                                                 TextChanged="SearchTextBox_TextChanged"/>
                                    </Grid>
                                    <Button Grid.Column="1"
                                            x:Name="RefreshButton"
                                            Style="{StaticResource RefreshButtonInBarStyle}"
                                            Click="RefreshButton_Click"
                                            ClickMode="Press"
                                            Focusable="False"
                                            Width="28"
                                            Height="28"
                                            Foreground="{StaticResource TextSecondaryBrush}"
                                            Padding="0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            ToolTip="Recharger la liste des processus"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <CheckBox x:Name="ShowAllProcessesCheckBox"
                                  Grid.Row="1"
                                  Style="{StaticResource CheckBoxModern}"
                                  Content="Inclure les processus sans fenêtre visible"
                                  Margin="0,0,0,8"
                                  Checked="ShowAllProcessesCheckBox_Changed"
                                  Unchecked="ShowAllProcessesCheckBox_Changed"/>
                        <Border Grid.Row="2"
                                Background="{StaticResource BackgroundPrimaryBrush}"
                                BorderBrush="{StaticResource BorderLightBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource RadiusTextInput}"
                                MinHeight="280"
                                ClipToBounds="False"
                                Padding="4">
                            <Grid>
                                <ListView x:Name="ProcessListView"
                                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                      ScrollViewer.CanContentScroll="False"
                                      SizeChanged="ProcessListView_SizeChanged"
                                      SelectionChanged="ProcessListView_SelectionChanged"
                                      SelectionMode="Multiple"
                                      Background="{StaticResource BackgroundPrimaryBrush}"
                                      BorderThickness="0"
                                      Padding="4"
                                      Margin="0,0,0,12"
                                      Visibility="Collapsed"
                                      IsEnabled="False">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="MinHeight" Value="28"/>
                                        <Setter Property="Padding" Value="4,2"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListViewItem">
                                                    <Border x:Name="RowBorder"
                                                            Background="{TemplateBinding Background}"
                                                            Padding="{TemplateBinding Padding}"
                                                            HorizontalAlignment="Stretch"
                                                            VerticalAlignment="Stretch"
                                                            Margin="0">
                                                        <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                                               HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                               VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="RowBorder" Property="Background" Value="#15FFFFFF"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="RowBorder" Property="Background" Value="#15FFFFFF"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Width="32">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                  Style="{StaticResource CheckBoxModern}"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Click="ProcessCheckBox_Click"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn x:Name="ProcessusColumn" Header="Processus" Width="180">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="-4,0,0,0">
                                                        <Grid Width="16" Height="16" Margin="0,0,8,0">
                                                            <Border Background="{StaticResource BackgroundTertiaryBrush}"
                                                                    CornerRadius="2"
                                                                    BorderBrush="{StaticResource BorderMediumBrush}"
                                                                    BorderThickness="1"/>
                                                            <Image Source="{Binding Icon}"
                                                                   Width="16" Height="16"
                                                                   Stretch="Uniform"
                                                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                                        </Grid>
                                                        <TextBlock Text="{Binding ProcessName}" Style="{StaticResource TextBody}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn x:Name="FenetreColumn" Header="Fenêtre" Width="280">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding WindowTitle}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="-4,0,0,0" TextTrimming="CharacterEllipsis"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn x:Name="PidColumn" Header="PID" Width="68">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding ProcessId}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="-4,0,0,0"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                                <!-- Indicateur de chargement (au-dessus, masqué quand la liste est prête) -->
                                <Border x:Name="LoadingOverlay"
                                        Background="{StaticResource BackgroundPrimaryBrush}"
                                        Visibility="Visible">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="&#xE087;"
                                                   FontFamily="{StaticResource FontLucide}"
                                                   FontSize="32"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,12"/>
                                        <TextBlock Text="Chargement des processus..."
                                                   Style="{StaticResource TextMuted}"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- Sélection actuelle : pleine largeur, s’adapte au contenu (pas de rognage, pas de scroll) -->
            <Border Grid.Row="2"
                    Background="{StaticResource BackgroundTertiaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="20,12"
                    Height="160">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock x:Name="SelectionTitleText"
                                   Text="Sélection actuelle"
                                   Style="{StaticResource TextHeading}"
                                   FontSize="13"
                                   VerticalAlignment="Center"/>
                        <TextBlock x:Name="SelectionCountText"
                                   Style="{StaticResource TextMuted}"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   Margin="6,0,0,0"/>
                    </StackPanel>
                    <ScrollViewer Grid.Row="1"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Disabled"
                                 Margin="0,0,0,8"
                                 CanContentScroll="False">
                        <WrapPanel x:Name="SelectedAppsPanel"
                                   MinHeight="24"/>
                    </ScrollViewer>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="Ajouter un processus par nom :"
                                   Style="{StaticResource TextSecondary}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"
                                   TextTrimming="CharacterEllipsis"/>
                        <Border x:Name="AddProcessBarBorder" Grid.Column="1"
                                Background="{StaticResource BackgroundPrimaryBrush}"
                                BorderBrush="{StaticResource BorderLightBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource RadiusTextInput}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0" Margin="4,0,0,0">
                                    <TextBlock x:Name="AddProcessPlaceholder"
                                               Text="Ex. : notepad, chrome (sans .exe)"
                                               Style="{StaticResource TextMuted}"
                                               FontSize="12"
                                               IsHitTestVisible="False"
                                               VerticalAlignment="Center"
                                               Margin="2,0,0,0"/>
                                    <TextBox x:Name="ManualProcessTextBox"
                                             Style="{StaticResource TextBoxSearchInBar}"
                                             Background="Transparent"
                                             Foreground="{StaticResource TextPrimaryBrush}"
                                             CaretBrush="{StaticResource TextPrimaryBrush}"
                                             FontSize="12"
                                             Padding="0,2,0,2"
                                             VerticalAlignment="Center"
                                             KeyDown="ManualProcessTextBox_KeyDown"
                                             GotFocus="ManualProcessTextBox_GotFocus"
                                             LostFocus="ManualProcessTextBox_LostFocus"
                                             TextChanged="ManualProcessTextBox_TextChanged"/>
                                </Grid>
                                <Button Grid.Column="1"
                                        x:Name="AddProcessButton"
                                        Style="{StaticResource RefreshButtonInBarStyle}"
                                        Click="AddManualProcess_Click"
                                        ClickMode="Press"
                                        Focusable="False"
                                        Width="28"
                                        Height="28"
                                        Foreground="{StaticResource TextSecondaryBrush}"
                                        Padding="0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        ToolTip="Ajouter le processus"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- Pied : Annuler / Valider -->
            <Border Grid.Row="3"
                    Background="{StaticResource BackgroundSecondaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="20,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Annuler"
                            Style="{StaticResource ButtonSecondary}"
                            Click="CancelButton_Click"
                            ClickMode="Press"
                            Focusable="False"
                            Width="100"
                            Margin="0,0,10,0"/>
                    <Button Content="Valider"
                            Style="{StaticResource ButtonPrimary}"
                            Click="OkButton_Click"
                            ClickMode="Press"
                            Focusable="False"
                            Width="100"
                            IsDefault="True"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
