<Window x:Class="MacroEngine.UI.ProfilesManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Gérer les Profils"
        Width="800"
        Height="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        MinWidth="600" MinHeight="400"
        Background="{StaticResource BackgroundPrimaryBrush}">
    <Border x:Name="RootBorder"
            Background="{StaticResource BackgroundPrimaryBrush}"
            BorderBrush="{StaticResource BorderLightBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barre de titre -->
            <Border x:Name="TitleBarBorder" Grid.Row="0"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Background="{StaticResource BackgroundSecondaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="16,0,0,0" Height="44"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="&#xE0D7;" FontFamily="{StaticResource FontLucide}" FontSize="18"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="Gérer les Profils" Style="{StaticResource TextHeading}"
                                   Foreground="{StaticResource TextPrimaryBrush}" FontSize="15"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="MinimizeButton" Style="{StaticResource WindowCaptionButtonStyle}"
                                ClickMode="Press" Focusable="False" Foreground="{StaticResource TextPrimaryBrush}"
                                FontFamily="{StaticResource FontLucide}" Content="&#xE11C;"
                                ToolTip="Réduire" Click="MinimizeButton_Click"/>
                        <Button x:Name="MaximizeButton" Style="{StaticResource WindowCaptionButtonStyle}"
                                ClickMode="Press" Focusable="False" Foreground="{StaticResource TextPrimaryBrush}"
                                FontFamily="{StaticResource FontLucide}" Content="&#xE167;"
                                ToolTip="Agrandir" Click="MaximizeButton_Click"/>
                        <Button x:Name="CloseButton" Style="{StaticResource WindowCaptionCloseButtonStyle}"
                                ClickMode="Press" Focusable="False" Foreground="{StaticResource TextPrimaryBrush}"
                                FontFamily="{StaticResource FontLucide}" Content="&#xE1B2;"
                                ToolTip="Fermer" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Contenu : liste à gauche, éditeur à droite -->
            <Grid Grid.Row="1" Margin="20,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" MinWidth="160"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Colonne gauche : liste des profils (occupe toute la largeur de la colonne) -->
                <Grid Grid.Column="0" HorizontalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Button x:Name="NewProfileButton" Grid.Row="0" Style="{StaticResource ButtonPrimary}"
                            Click="NewProfileButton_Click" Margin="0,0,0,12" Padding="12,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE081;" FontFamily="{StaticResource FontLucide}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Nouveau profil" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Border Grid.Row="1" Background="{StaticResource BackgroundSecondaryBrush}"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="8"
                            MinHeight="120" MaxHeight="400" VerticalAlignment="Top"
                            HorizontalAlignment="Stretch">
                        <ListBox x:Name="ProfilesListBox" Background="Transparent" BorderThickness="0"
                                 HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                                 SelectionMode="Single" SelectionChanged="ProfilesListBox_SelectionChanged"
                                 MouseDoubleClick="ProfilesListBox_MouseDoubleClick"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.CanContentScroll="False">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="10,8" Margin="0,0,0,4" CornerRadius="6"
                                            Background="{StaticResource BackgroundTertiaryBrush}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Border Grid.Column="1" CornerRadius="4" Padding="6,2" VerticalAlignment="Center"
                                                    Background="{StaticResource SuccessBrush}" Margin="8,0,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="En cours" FontSize="10" Foreground="White"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                    <TextBlock x:Name="StatusText" Grid.Row="2" Foreground="{StaticResource ErrorBrush}" FontSize="12"
                               TextWrapping="Wrap" Margin="0,8,0,0" Visibility="Collapsed"/>
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button x:Name="ActivateProfileButton" Style="{StaticResource ButtonSecondary}"
                                Click="ActivateProfileButton_Click" Margin="0,0,6,0" Padding="8,6"
                                IsEnabled="False" ToolTip="Utiliser ce profil">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE080;" FontFamily="{StaticResource FontLucide}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Utiliser" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeleteProfileButton" Style="{StaticResource ButtonGhost}"
                                Click="DeleteProfileButton_Click" Padding="8,6" IsEnabled="False">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE18E;" FontFamily="{StaticResource FontLucide}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Supprimer" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- Colonne droite : éditeur ou placeholder -->
                <Border Grid.Column="2" Background="{StaticResource BackgroundSecondaryBrush}"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="20">
                    <Grid>
                        <ContentControl x:Name="EditorContent"/>
                        <TextBlock x:Name="PlaceholderText"
                                  Text="Sélectionnez un profil dans la liste ou créez-en un nouveau."
                                  Style="{StaticResource TextMuted}" FontSize="14"
                                  VerticalAlignment="Center" HorizontalAlignment="Center"
                                  TextWrapping="Wrap" TextAlignment="Center" MaxWidth="320"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
