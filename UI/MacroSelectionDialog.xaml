<Window x:Class="MacroEngine.UI.MacroSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sélectionner des Macros" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Sélectionnez les macros à ajouter au profil:" 
                   Margin="0,0,0,10"/>
        
        <ListBox x:Name="MacrosListBox" Grid.Row="1" SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Description}" FontSize="10" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" x:Name="OkButton" Click="Ok_Click" Margin="0,0,5,0" Padding="10,5" Width="75"/>
            <Button Content="Annuler" x:Name="CancelButton" Click="Cancel_Click" Padding="10,5" Width="75"/>
        </StackPanel>
    </Grid>
</Window>


