<Window x:Class="MacroEngine.UI.ShortcutCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Définir un raccourci"
        Height="200"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="{StaticResource BackgroundPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="InstructionText"
                   Grid.Row="0"
                   Text="Appuyez sur une touche pour définir le raccourci de cette macro."
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,12"/>

        <Border Grid.Row="1"
                Background="{StaticResource BackgroundTertiaryBrush}"
                BorderBrush="{StaticResource BorderLightBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="14,12"
                VerticalAlignment="Center">
            <TextBlock x:Name="KeyHintText"
                       Text="(F1–F12, ou toute autre touche… — Échap pour annuler)"
                       FontSize="12"
                       Foreground="{StaticResource TextMutedBrush}"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <TextBlock Grid.Row="2"
                   Text="Conseil : les touches de fonction (F1–F12) sont pratiques pour les raccourcis de macro."
                   FontSize="11"
                   Foreground="{StaticResource TextMutedBrush}"
                   TextWrapping="Wrap"
                   Margin="0,12,0,0"/>
    </Grid>
</Window>
