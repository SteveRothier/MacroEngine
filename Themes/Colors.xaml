<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══════════════════════════════════════════════════════════════════════
         MacroEngine - Palette de Couleurs
         Design: Beige professionnel avec accents vert sauge / bleu pétrole
         ══════════════════════════════════════════════════════════════════════ -->

    <!-- Fonds principaux -->
    <Color x:Key="BackgroundPrimaryColor">#F4EDE4</Color>
    <Color x:Key="BackgroundSecondaryColor">#EFE6DA</Color>
    <Color x:Key="BackgroundTertiaryColor">#E8DFD3</Color>
    
    <!-- Accents -->
    <Color x:Key="AccentPrimaryColor">#6FA8A1</Color>
    <Color x:Key="AccentSecondaryColor">#4A6C6F</Color>
    <Color x:Key="AccentHoverColor">#5D9690</Color>
    <Color x:Key="AccentPressedColor">#3D5A5D</Color>
    
    <!-- Texte -->
    <Color x:Key="TextPrimaryColor">#2E2E2E</Color>
    <Color x:Key="TextSecondaryColor">#5A5A5A</Color>
    <Color x:Key="TextMutedColor">#8A8A8A</Color>
    <Color x:Key="TextOnAccentColor">#FFFFFF</Color>
    
    <!-- États -->
    <Color x:Key="WarningColor">#D8A24A</Color>
    <Color x:Key="ErrorColor">#C94A4A</Color>
    <Color x:Key="SuccessColor">#5A9F68</Color>
    <Color x:Key="InfoColor">#5A8AC9</Color>
    
    <!-- Bordures -->
    <Color x:Key="BorderLightColor">#D4C9BC</Color>
    <Color x:Key="BorderMediumColor">#C4B9AC</Color>
    <Color x:Key="BorderDarkColor">#A49989</Color>
    
    <!-- Ombres -->
    <Color x:Key="ShadowColor">#1A000000</Color>
    <Color x:Key="ShadowHoverColor">#2E000000</Color>

    <!-- ══════════════════════════════════════════════════════════════════════
         Couleurs des Blocs (Scratch-like)
         ══════════════════════════════════════════════════════════════════════ -->
    
    <!-- Bloc Clavier - Bleu -->
    <Color x:Key="BlockKeyboardColor">#5B8DEF</Color>
    <Color x:Key="BlockKeyboardDarkColor">#4A7BD9</Color>
    
    <!-- Bloc Souris - Vert -->
    <Color x:Key="BlockMouseColor">#5ABB7A</Color>
    <Color x:Key="BlockMouseDarkColor">#4AA66A</Color>
    
    <!-- Bloc Délai - Jaune/Orange -->
    <Color x:Key="BlockDelayColor">#F5A623</Color>
    <Color x:Key="BlockDelayDarkColor">#E09515</Color>
    
    <!-- Bloc Boucle/Répétition - Violet -->
    <Color x:Key="BlockLoopColor">#9B6DD8</Color>
    <Color x:Key="BlockLoopDarkColor">#8A5DC8</Color>
    
    <!-- Fond de l'éditeur avec grille -->
    <Color x:Key="EditorGridColor">#E0D6C8</Color>

    <!-- ══════════════════════════════════════════════════════════════════════
         Brushes (SolidColorBrush)
         ══════════════════════════════════════════════════════════════════════ -->
    
    <!-- Fonds -->
    <SolidColorBrush x:Key="BackgroundPrimaryBrush" Color="{StaticResource BackgroundPrimaryColor}"/>
    <SolidColorBrush x:Key="BackgroundSecondaryBrush" Color="{StaticResource BackgroundSecondaryColor}"/>
    <SolidColorBrush x:Key="BackgroundTertiaryBrush" Color="{StaticResource BackgroundTertiaryColor}"/>
    
    <!-- Accents -->
    <SolidColorBrush x:Key="AccentPrimaryBrush" Color="{StaticResource AccentPrimaryColor}"/>
    <SolidColorBrush x:Key="AccentSecondaryBrush" Color="{StaticResource AccentSecondaryColor}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
    <SolidColorBrush x:Key="AccentPressedBrush" Color="{StaticResource AccentPressedColor}"/>
    
    <!-- Texte -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
    <SolidColorBrush x:Key="TextOnAccentBrush" Color="{StaticResource TextOnAccentColor}"/>
    
    <!-- États -->
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>
    
    <!-- Bordures -->
    <SolidColorBrush x:Key="BorderLightBrush" Color="{StaticResource BorderLightColor}"/>
    <SolidColorBrush x:Key="BorderMediumBrush" Color="{StaticResource BorderMediumColor}"/>
    <SolidColorBrush x:Key="BorderDarkBrush" Color="{StaticResource BorderDarkColor}"/>
    
    <!-- Blocs Scratch-like -->
    <SolidColorBrush x:Key="BlockKeyboardBrush" Color="{StaticResource BlockKeyboardColor}"/>
    <SolidColorBrush x:Key="BlockKeyboardDarkBrush" Color="{StaticResource BlockKeyboardDarkColor}"/>
    <SolidColorBrush x:Key="BlockMouseBrush" Color="{StaticResource BlockMouseColor}"/>
    <SolidColorBrush x:Key="BlockMouseDarkBrush" Color="{StaticResource BlockMouseDarkColor}"/>
    <SolidColorBrush x:Key="BlockDelayBrush" Color="{StaticResource BlockDelayColor}"/>
    <SolidColorBrush x:Key="BlockDelayDarkBrush" Color="{StaticResource BlockDelayDarkColor}"/>
    <SolidColorBrush x:Key="BlockLoopBrush" Color="{StaticResource BlockLoopColor}"/>
    <SolidColorBrush x:Key="BlockLoopDarkBrush" Color="{StaticResource BlockLoopDarkColor}"/>
    <SolidColorBrush x:Key="EditorGridBrush" Color="{StaticResource EditorGridColor}"/>

    <!-- ══════════════════════════════════════════════════════════════════════
         Effets d'ombre
         ══════════════════════════════════════════════════════════════════════ -->
    
    <!-- Ombre subtile pour les cartes/panneaux -->
    <DropShadowEffect x:Key="ShadowSubtle" 
                      Color="Black" 
                      Opacity="0.08" 
                      BlurRadius="8" 
                      ShadowDepth="2"
                      Direction="270"/>
    
    <!-- Ombre pour les blocs d'action (normal) -->
    <DropShadowEffect x:Key="ShadowActionBlock" 
                      Color="Black" 
                      Opacity="0.12" 
                      BlurRadius="8" 
                      ShadowDepth="2"
                      Direction="270"/>
    
    <!-- Ombre pour les blocs d'action (hover) -->
    <DropShadowEffect x:Key="ShadowActionBlockHover" 
                      Color="Black" 
                      Opacity="0.18" 
                      BlurRadius="12" 
                      ShadowDepth="3"
                      Direction="270"/>
    
    <!-- Ombre pour les boutons (hover) -->
    <DropShadowEffect x:Key="ShadowButtonHover" 
                      Color="Black" 
                      Opacity="0.15" 
                      BlurRadius="6" 
                      ShadowDepth="2"
                      Direction="270"/>

    <!-- ══════════════════════════════════════════════════════════════════════
         CornerRadius (Border Radius)
         ══════════════════════════════════════════════════════════════════════ -->
    
    <CornerRadius x:Key="RadiusActionBlock">12</CornerRadius>
    <CornerRadius x:Key="RadiusButton">10</CornerRadius>
    <CornerRadius x:Key="RadiusPanel">14</CornerRadius>
    <CornerRadius x:Key="RadiusTextInput">8</CornerRadius>
    <CornerRadius x:Key="RadiusWindow">16</CornerRadius>
    <CornerRadius x:Key="RadiusSmall">4</CornerRadius>

</ResourceDictionary>
