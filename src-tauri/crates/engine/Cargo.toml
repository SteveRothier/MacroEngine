[package]
name = "macroengine-engine"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "MacroEngine core engine contracts (state, cancel, actions)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
